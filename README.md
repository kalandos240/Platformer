# Temporary Pingus WebAssembly builder

Public CI workspace used to compile the GPL-licensed original Pingus 0.7.6 source into a browser build for Yandex Games.

Active target: validated `pingus-yandex-web.zip` with `index.html` in the archive root.
